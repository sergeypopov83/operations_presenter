apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "operations_presenter.fullname" . }}-test-connection"
  labels:
    {{- include "operations_presenter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "operations_presenter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
