apiVersion: v1
kind: Service
metadata:
  name: {{ include "operations_presenter.fullname" . }}
  labels:
    {{- include "operations_presenter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "operations_presenter.selectorLabels" . | nindent 4 }}
