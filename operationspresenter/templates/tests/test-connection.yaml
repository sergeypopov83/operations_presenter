apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "operationspresenter.fullname" . }}-test-connection"
  labels:
    {{- include "operationspresenter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "operationspresenter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
